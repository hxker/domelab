<h1 class="page-header">
  编辑<%= t('activerecord.models.school') %>
  <small class="pull-right">
    <a href="<%= admin_school_path(@school) %>" title="显示该<%= t('activerecord.models.school') %>" class="btn btn-default">
      <i class="icon-eye-open icon-only"></i></a>
    <a href="<%= admin_schools_path %>" title="<%= t('activerecord.models.school') %>列表" class="btn btn-default">
      <i class="icon-list icon-only"></i></a>
  </small>
</h1>

<%= render 'form' %>
