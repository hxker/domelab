<h1 class="page-header">
  编辑<%= t('activerecord.models.course') %>
  <small class="pull-right">
    <a href="<%= admin_course_path(@course) %>" title="显示该<%= t('activerecord.models.course') %>" class="btn btn-default">
      <i class="icon-eye-open icon-only"></i></a>
    <a href="<%= admin_courses_path %>" title="<%= t('activerecord.models.course') %>列表" class="btn btn-default">
      <i class="icon-list icon-only"></i></a>
  </small>
</h1>

<%= render 'form' %>
